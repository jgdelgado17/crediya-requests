##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: crediya-requests
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    username: "postgres"
    password: "postgres"
    database: "crediya_requests"
    schema: "public"
